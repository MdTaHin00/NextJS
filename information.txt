'use client'
 client page kas koray  -> 'use client' deta hoy
 ar 'use client' ai word first line deta hoy

2-> 2-routing-method
   auth
   {/* (auth) -> folder a () use kolar janno url a auth deta hova na */}
   {/* url search : localhost:3000/login  */}
   {/* url search : localhost:3000/register  */}

   2dynamicroute
   [id] -> [] ja name value use kovo ta dynamic value hova , same name use korta hova

   4catchSegments 
   [...value] -> CatchSegments Route , value same name use korta hoy

   5catchAllSegment
   [[...catch]] -> Catching ALL Segments , all catch search value asa , same name use korta hoy

   6private_folder
   _book -> kono folder ar first ( _ ) user kola private folder hoy
   
4-> 4 a advanced-dashboard ai method

   @user -> kono folder ar name agak @ ata dela sai folder search ar kas korva na 
   @-> user method reload data nan tai (default.tsx) ai file all code add kora

   intercepting route  
   (.) -> same level ar janno
   (..) one level ar janno
   (..)(..) two level ar janno 
   (...) root app directory
   intercepting Route -> use korlay same name anno folder kas / show korva na , sodu (.)/(..)/(...) folder name kas kola
